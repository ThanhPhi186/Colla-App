import {getBottomSpace} from '../../../helpers/iphoneXHelper';

const styles = {
  btnGoCart: {
    alignSelf: 'center',
    position: 'absolute',
    bottom: getBottomSpace(),
  },
};
export default styles;
